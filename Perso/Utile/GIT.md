Supprimer une branche du un répo distant : 
> `git push <remote-branch> --delete <branch-name>`
> la plus part du temps remote-branch est origin

Supprimer une branche en local 
> `git branch -d <branch-name
